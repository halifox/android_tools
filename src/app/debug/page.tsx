'use client'


import {Adb} from "@yume-chan/adb"
import {useRef} from "react"

export default function Home() {
    const adbRef = useRef<Adb | undefined>(null)
    return (
        <div>

        </div>
    )
}
